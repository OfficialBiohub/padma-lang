# Text and math helpers
let words = text.split("Padma,বাংলা,Termux", ",")
print text.join(words, " | ")
print text.upper("padma")
print math.max(3, 8, 5)

# Safe relative-file access
file.write("notes.txt", "Padma works on Termux")
if file.exists("notes.txt") {
  print file.read("notes.txt")
}

# Seconds since the Unix epoch; sleep is limited to 60 seconds per call.
print time.now() > 0
time.sleep(0)
